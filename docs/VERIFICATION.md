# Execution and validation evidence

All **16 code cells** were executed in order in a fresh local
IPython process, starting from an empty working directory. The notebook cells were
used unchanged, including package installation and embedded-data loading.

- Error outputs: **0**.
- Actual captured output blocks: **46**.
- Actual captured PNG figures: **10**.
- Execution time with dependencies already available: **19.84 seconds**.
- The saved notebook and HTML report include those actual outputs.
- The selected-data SHA-256, schema, complete daily index, null checks, and nonnegative-value checks passed.
- Both causal feature invariance and recursive lag-source checks passed.
- The final export cell's nine integrity checks passed.
- Representative ACF/PACF, model-comparison, and final-interval figures were visually inspected.

This was **not an actual authenticated Google Colab session**. The host did not
permit Jupyter TCP or IPC socket binding. Execution therefore used IPython's real
cell execution, MIME display capture, and inline matplotlib renderer within one
fresh local process, without a socket-based notebook server. No outputs or results
were fabricated. The notebook is designed for fresh Colab with its own standard
setup cell, but actual Colab environment compatibility remains subject to the
supported package ranges and Google's runtime.

The first execution captured only plain-text representations because the terminal
display formatter had disabled rich MIME types. A second complete execution enabled
HTML and PNG formatting and captured the real tables and figures. Model settings,
dates, and forecast values were not tuned between these executions.

Package versions are in `requirements-tested.txt` and `results/run_config.json`.
The numerical results are generated by the notebook, not copied from course examples.
Machine-specific fit/inference timings may vary on rerun.

The prose summary was filled after execution using `results_summary.json`.
It interprets this saved configuration; if data or settings change, rerun and update
the interpretation before submitting. Actual cohort dates and GitHub publication
are explicitly unconfirmed and require the trainee's real information/account.
